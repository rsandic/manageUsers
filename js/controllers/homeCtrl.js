app.controller('HomeController', ["$rootScope", "$scope", "AuthService", "AUTH_EVENTS",'$state',
    function($rootScope, $scope, AuthService, AUTH_EVENTS, $state) {
        console.log("Home ctrl");
    }
]);
